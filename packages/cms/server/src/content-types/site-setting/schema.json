{
  "kind": "singleType",
  "collectionName": "site_setting",
  "info": {
    "singularName": "site-setting",
    "pluralName": "site-settings",
    "displayName": "Site Settings",
    "description": "Whitelabel identity, basic theme tokens, and block-composed site chrome served at runtime by the press engine"
  },
  "options": { "draftAndPublish": false },
  "pluginOptions": {},
  "attributes": {
    "basicSettings": { "type": "component", "repeatable": false, "component": "preset-config.basic-settings" },
    "layout": { "type": "component", "repeatable": false, "component": "preset-config.layout" },
    "pageDefaults": {
      "type": "customField",
      "customField": "plugin::press-cms.builder",
      "options": { "mode": "slots" }
    },
    "examplePlugin": { "type": "component", "repeatable": false, "component": "preset-config.example-plugin" },
    "seo": { "type": "component", "repeatable": false, "component": "preset-config.seo" },
    "legalPages": { "type": "component", "repeatable": false, "component": "preset-config.legal-pages" },
    "cookieConsent": { "type": "component", "repeatable": false, "component": "preset-config.cookie-consent" }
  },
  "config": {
    "settings": { "bulkable": true, "filterable": true, "pageSize": 10, "searchable": true, "mainField": "id" },
    "metadatas": {
      "basicSettings": { "edit": { "label": "Ajustes básicos", "description": "Core identity and the theme tokens most sites customize first." } },
      "layout": { "edit": { "label": "Layout", "description": "Baseline layout every page, row and column inherits unless a node overrides it in the Composition builder. Leave a field empty to keep the engine default." } },
      "pageDefaults": { "edit": { "label": "Page defaults", "description": "The default header and footer every page inherits. Pages can override or drop them per-slot." } },
      "examplePlugin": { "edit": { "label": "Example Plugin", "description": "Reference plugin proving the plugin framework end to end. Ships disabled." } },
      "seo": { "edit": { "label": "SEO", "description": "Head metadata defaults for every page — title template, description, share image, and social profiles." } },
      "legalPages": { "edit": { "label": "Legal Pages", "description": "Gates the seeded Privacy Policy page on a fresh install." } },
      "cookieConsent": { "edit": { "label": "Cookie Consent", "description": "The cookie-consent banner shown to visitors — copy, categories, and the enabled toggle." } }
    }
  }
}
